# Public routes are fixture-only. These values are used only by the local,
# explicitly invoked live-evidence command.
ENABLE_OPENAI_LIVE=false
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
OPENAI_REASONING_EFFORT=medium
