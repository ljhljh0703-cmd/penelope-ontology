# Fixture mode is the no-cost, deterministic default used by tests and reviewers.
NARRATIVE_MODEL_MODE=fixture

# Live mode requires a real key and must use GPT-5.6 for Build Week evidence.
ENABLE_OPENAI_LIVE=false
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
OPENAI_REASONING_EFFORT=medium
