/**
 * Automatic free-text-to-A/B matching was removed deliberately.
 *
 * The Story Workbench executes only a server-presented choice with its exact
 * choiceId. Creator-written directions belong to the World Workbench C
 * interview, where the proposed canonical execution is shown and confirmed
 * before state can change. Do not add an implicit intent matcher here.
 */
export {};
